import { describe, it } from "vitest";

describe("run command", () => {
    it("works as expected for correct input", () => {});

    it("fails is username is not provided", () => {});

    it("fails is event limit is incorrect", () => {});
});
